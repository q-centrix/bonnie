{{#if summary}}
  <label><span class="sr-only">{{#if cms_id}}{{cms_id}}, {{/if}}{{summary.percent}}% patients meet expected</span>
    <input type="text" value="{{summary.percent}}"  class="dial" data-readOnly="true" data-width="60" data-height="60" data-thickness=".2" data-fgColor={{color}} data-inputColor="#444" data-font="Helvetica Neue, Roboto, Helvetica, Arial, sans-serif">
  </label>
{{else}}
  {{! FIXME: Add a spinner here }}
  <div style="height: 65px;"></div>
{{/if}}
